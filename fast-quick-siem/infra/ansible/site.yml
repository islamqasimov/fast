---
# Master playbook - bütün stack-i düzgün sırayla quraşdırır.
# İstifadə: ansible-playbook site.yml
#
# Sıra vacibdir:
#   1. Wazuh Manager əvvəlcə ayağa qalxmalıdır (digərləri ona qoşulur)
#   2. IOC Collector - manager IP-sini bilməlidir (CDB push üçün)
#   3. Linux Target - manager IP-sini bilməlidir (agent registration üçün)

- import_playbook: playbook-wazuh-manager.yml
- import_playbook: playbook-ioc-integration.yml
- import_playbook: playbook-agents.yml
